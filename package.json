{
  "name": "persons",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "ruslan hamidov",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../fsopen/part3/phonebook && npm run build && @powershell Copy-Item dist -Recurse ../../../phonebook_be",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.8.0",
    "morgan": "^1.11.0"
  }
}
